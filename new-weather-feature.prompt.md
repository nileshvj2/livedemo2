---
name: new-weather-feature  
description: Adds a new weather feature to the app following project standards
---

Add a new feature to the weather app.

Steps:
1. Add the data-fetch function in `weather.py` with type hints and error handling
2. Add the Streamlit UI for it in `streamlitapp.py`  
3. Write a pytest test in `test_app.py`
4. Follow all rules in `.github/copilot-instructions.md`

Feature to add: $FEATURE_DESCRIPTION